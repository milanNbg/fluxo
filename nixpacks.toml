[phases.setup]
nixPkgs = ["nodejs_22", "pnpm-9_x"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = [
    "pnpm --filter @fluxo/api exec prisma generate",
    "pnpm --filter @fluxo/api build"
]

[start]
cmd = "pnpm --filter @fluxo/api start"