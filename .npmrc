# Use exact versions when installing packages
save-exact=false

# Prevent installation if engine requirements aren't met
engine-strict=true

# Hoist common dependencies to root for better deduplication
shamefully-hoist=false

# Show full error stack traces
loglevel=info

# Auto-install peer dependencies
auto-install-peers=true

# Enforce strict peer dependency resolution
strict-peer-dependencies=false