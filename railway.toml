[build]
builder = "RAILPACK"
buildCommand = "pnpm install --frozen-lockfile && pnpm --filter @fluxo/api exec prisma generate && pnpm --filter @fluxo/api build"

[deploy]
startCommand = "pnpm --filter @fluxo/api start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3